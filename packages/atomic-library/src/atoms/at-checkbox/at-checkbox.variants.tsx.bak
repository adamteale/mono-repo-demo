import { cva } from 'class-variance-authority'

const baseClass = ''

export const checkboxVariants = cva(baseClass, {
  variants: {
    disabled: {
      true: 'text-feedback-default',
      false: 'text-primary',
    },
  },
  defaultVariants: {
    disabled: false,
  },
})
