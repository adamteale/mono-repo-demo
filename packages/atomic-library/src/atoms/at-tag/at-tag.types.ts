export interface AtTagProps {
  /** Additional CSS classes to apply custom styling to the tag. */
  className?: string

  /** A string representing the text content of the tag. This is the primary information displayed within the tag. */
  text: string

  /** Additional CSS classes to apply custom styling to the text on the tag. */
  textClassName?: string
}
