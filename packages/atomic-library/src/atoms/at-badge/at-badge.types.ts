import { View } from "react-native";export interface AtBadgeProps {
  /** Additional CSS class name for the component. */
  className?: string

  /** The data test ID for testing purposes. */
  dataTestId?: string

  /** The quantity to be displayed in the badge. */
  quantity: number
}
