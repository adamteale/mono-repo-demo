import { MlMediaProps } from '../ml-media'

export interface MlVideoPlayerProps {
  /** The URL of the video to be played. This property is required. */
  videoUrl: string

  /** Optional properties for the thumbnail image of the video. This includes the source URL, alt text, and other relevant media properties as defined by the `MlMediaProps` interface. */
  thumbnail?: MlMediaProps
}
