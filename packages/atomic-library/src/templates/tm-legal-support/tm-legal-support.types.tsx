import { ReactNode } from 'react'
import { OrSidebarMenuProps } from '../../organisms'

export interface TmLegalSupportProps {
  title: string
  menuProps: OrSidebarMenuProps
  children?: ReactNode
}
