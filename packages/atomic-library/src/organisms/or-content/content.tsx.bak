import { OrContentProps } from "./content.types";

export const OrContent = ({ title, children, iconLinks }: OrContentProps) => {
  return (
    <View>
      {iconLinks}
      <Text className="text-primary font-bold text-xl pt-8 pb-8 md:text-2xl xl:pt-10 xl:pb-10">
        {title}
      </Text>
      {children}
    </View>
  );
};
