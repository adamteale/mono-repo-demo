import * as React from "react";

interface Props {
  children?: React.ReactNode;
}

export const {{ pascalCase name }} = ({ children }: Props) => {
  return (
    <View>
      <h1>{{ name }}</h1>
      {children}
    </View>
  );
};
