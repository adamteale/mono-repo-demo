// import { environment } from "../config/environment";

// export const getStaticPath = (path: string) => `${environment.staticPathPrefix}${path}`
export const getStaticPath = (path: string) => "REPLACE_ME";
